package io.github.mtslohn.todolist.controller;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGraphQLIT extends TasksControllerTest {

    // Execute the same tests but in native mode.
}